# dependencies
/node_modules
/.pnp
.pnp.*

# next.js build output
/.next/
/out/
/build

# env files (never commit secrets)
.env*

# typescript
*.tsbuildinfo
next-env.d.ts

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store
*.pem
.vercel

# local-only Claude Code settings
.claude/settings.local.json

# runtime data (content edits, enquiries, uploaded images) — lives on the server, back it up separately
/data/
